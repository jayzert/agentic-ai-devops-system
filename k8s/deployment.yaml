apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-agent
  template:
    metadata:
      labels:
        app: devops-agent
    spec:
      containers:
      - name: agent
        image: devops-agent:latest